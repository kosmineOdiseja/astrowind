---
import Item from '~/components/stories/GridItem.astro';

const { posts } = Astro.props;
// console.log(posts, 'this is my posts');
---

<!-- <div class="mx-auto md:flex md:flex-row md:max-w-5xl md:h-auto gap-4 justify-between items-center inline-block"> -->
<div class="flex flex-wrap md:flex-row max-w-6xl mx-auto md:gap-4">
	{posts.map((post) => <Item post={post} />)}
</div>
