---
import Grid from '~/components/stories/Grid.astro';
import { findPostsByIds } from '~/utils/stories';

const ids = [
	'gyvenimo-istorija-verute',
	'how-to-customize-astrowind-to-your-brand',
	'useful-resources-to-create-websites',
];

const posts = await findPostsByIds(ids);
---

<section class="px-4 py-16 mx-auto max-w-6xl md:flex md:flex-row md:justify-between md:items-center">
	<div class="mx-auto">
		<h2 class="flex"><span class="items-center mx-auto mb-8 text-2xl font-bold">Dienoraštis</span></h2>
		<Grid posts={posts} />
	</div>
</section>
