---
import Item from '~/components/blog/GridItem.astro';

const { posts } = Astro.props;
---

<div class="mx-auto md:flex md:flex-row justify-between items-center">
	{posts.map((post) => <Item post={post} />)}
</div>
